---
description: Open or close an isolated second-brain write session (worktree + PR).
---

Run the `sbc-session` skill. Follow its process exactly. Use deterministic scripts; do not invent schema fields.
