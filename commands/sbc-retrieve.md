---
description: Spawn a retrieval sub-agent for second-brain Q&A so pack stays off the parent.
---

Run the `sbc-retrieve` skill. Follow its process exactly. Use deterministic scripts; do not invent schema fields.
