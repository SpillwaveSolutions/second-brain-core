{
  "description": "Remind writers to use deterministic helpers and isolation sessions.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"print('Second brain: claim SECOND_BRAIN_IDENTITY; write only via pack scripts; isolate writes with brain_session.py open')\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
